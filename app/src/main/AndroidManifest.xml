<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MoneyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManageYourMoney"
        tools:targetApi="36">

        <!--
          Android 16 (API 36) notes:
          * enableEdgeToEdge() is called in MainActivity.onCreate — the manifest no longer
            needs (and API 36 ignores) android:windowOptOutEdgeToEdgeEnforcement.
          * No android:screenOrientation / resizeableActivity="false" locks are declared,
            since API 36 ignores orientation/aspect-ratio restrictions on large screens
            (>600dp) anyway — we embrace adaptive layouts instead of fighting the platform.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ManageYourMoney.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
